# Форма поиска QSO для QRZ.com (по owner_callsign)

## ✅ Что готово:

1. **API обновлён** - использует `owner_callsign` для получения user_id из RadioProfile
2. **Форма для QRZ.com** - с скрытым полем `owner_callsign`
3. **Тестовая страница** - для проверки работы

## 📋 Как это работает:

```
Пользователь на QRZ.com (страница R3LO)
    ↓
Вводит свой позывной (например, GJ0KYZ)
    ↓
API получает user_id по owner_callsign="R3LO" из RadioProfile
    ↓
Ищет QSO где user_id=1 AND callsign="GJ0KYZ"
    ↓
Возвращает все QSO (строки=my_callsign, колонки=modes)
```

## 🧪 Тестирование:

Откройте в браузере:
```
http://127.0.0.1:8080/test_qrz_user.html
```

**Тест 1:**
- Owner Callsign: `R3LO`
- Search Callsign: `GJ0KYZ`
- Результат: QSO R3LO с GJ0KYZ (10 связей)

**Тест 2:**
- Owner Callsign: `RA3DNC`
- Search Callsign: `GJ0KYZ`
- Результат: QSO RA3DNC с GJ0KYZ (20 связей) - **другой user_id!**

## 📝 Установка на QRZ.com:

1. Откройте файл `QRZ_COM_FORM_FOR_USER.md`

2. Измените строку:
   ```javascript
   const OWNER_CALLSIGN = 'R3LO';  // ЗАМЕНИТЕ НА ВАШ ПОЗЫВНОЙ
   ```

3. Скопируйте весь HTML/JavaScript код

4. Вставьте на страницу вашего профиля на QRZ.com в поле "Bio"

## 🔗 API Endpoint:

```
GET /api/v1/public/qso-search/?owner_callsign={owner}&search_callsign={search}
```

**Параметры:**
- `owner_callsign` - позывной владельца страницы (например, "R3LO")
- `search_callsign` - позывной корреспондента (например, "GJ0KYZ")

**Ответ:**
```json
{
    "found": true,
    "owner_callsign": "R3LO",
    "search_callsign": "GJ0KYZ",
    "results": [
        {
            "my_callsign": "R3LO",
            "modes": {
                "CW": 2,
                "FT8": 6,
                "FT4": 2
            }
        }
    ]
}
```

## ✅ Проверено:

- ✅ API использует RadioProfile для получения user_id
- ✅ Фильтрация по user_id работает (R3LO и RA3DNC возвращают разные результаты)
- ✅ CORS настроен для работы на qrz.com
- ✅ Форма работает с новыми параметрами

## 📁 Файлы:

- `QRZ_COM_FORM_FOR_USER.md` - код формы для вставки на QRZ.com
- `test_qrz_user.html` - тестовая страница
- `tlog/views/rest_api.py` - обновлённый API (PublicQSOSearchAPIView)
