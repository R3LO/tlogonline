# Быстрый тест API и формы

## ✅ Серверы запущены

- **Django API:** http://127.0.0.1:8000
- **HTTP Server:** http://127.0.0.1:8080

## 🧪 Простой тест (рекомендуется)

Откройте в браузере:
```
http://127.0.0.1:8080/test_simple.html
```

Эта страница:
- ✅ Не пытается читать CORS заголовки (браузер не позволяет это)
- ✅ Просто делает запрос к API
- ✅ Если запрос успешен — значит CORS настроен правильно
- ✅ Показывает результат в красивом виде

## 📋 Полный тест с формой

Откройте в браузере:
```
http://127.0.0.1:8080/test_qrz_form.html
```

**Важно:** Если показывает ошибку CORS, это нормально! JavaScript не может читать CORS заголовки.
Важно то, что **запрос проходит успешно** — значит CORS работает.

## 🔍 Тест через терминал

```bash
# Проверка API
curl "http://127.0.0.1:8000/api/v1/public/qso-search/?callsign=TEST"

# Проверка CORS (заголовки возвращаются)
curl -I -H "Origin: http://127.0.0.1:8080" \
  "http://127.0.0.1:8000/api/v1/public/qso-search/?callsign=TEST"
```

## ⚠️ О CORS заголовках

**JavaScript НЕ МОЖЕТ читать CORS заголовки** из соображений безопасности браузера.

Это нормально:
- ✅ Запрос проходит успешно
- ✅ Данные получены
- ✅ CORS работает

Это НЕ проблема:
- ❌ `Access-Control-Allow-Origin: null` в JavaScript
- ❌ Предупреждение "Заголовок Access-Control-Allow-Origin не найден"

## 🛑 Остановка серверов

```bash
pkill -f "python manage.py runserver"
pkill -f "python3 -m http.server 8080"
```

## 🚀 Повторный запуск

```bash
# Django
source venv/bin/activate
python manage.py runserver 127.0.0.1:8000

# HTTP сервер (в другом терминале)
python3 -m http.server 8080
```

## ✅ Когда всё работает

Если на `test_simple.html` вы видите:
```
✅ API работает! CORS настроен правильно.
{
  "found": false,
  "callsign": "TEST",
  "message": "Ничего не найдено"
}
```

Значит всё готово! Можете копировать код формы для QRZ.com.
